# Massless

- Read `MANIFEST.md` before editing. Systems listed there are finished: do not change their behaviour, tuning or structure unless the owner asks for that specific change in chat. Bug fixes there need the bug named first.
- Adding designs (`src/ships/`), modules (`src/fits.js`), mobs or HUD work is open, as long as the frozen files stay untouched.
- Dev server: `python3 serve.py` (port 8766). Tests: `node --test`.
